name: Regenerate client

on:
  schedule:
    - cron: '0 6 * * 1'   # Every Monday at 06:00 UTC
  workflow_dispatch:
    inputs:
      kanidm_version:
        description: 'Kanidm Docker image tag (e.g. "latest", "1.2.3")'
        required: false
        default: 'latest'

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Install openapi-generator-cli
        run: npm install -g @openapitools/openapi-generator-cli

      - name: Start Kanidm (ephemeral)
        run: |
          KANIDM_TAG="${{ github.event.inputs.kanidm_version || 'latest' }}"
          docker run -d --name kanidm \
            -p 8443:8443 \
            "kanidm/server:${KANIDM_TAG}"
          # Wait up to 60 seconds for the server to be ready.
          timeout 60 bash -c \
            'until curl -sk https://localhost:8443/status > /dev/null 2>&1; do sleep 2; done'

      - name: Download schema
        run: |
          curl -fsSk https://localhost:8443/docs/v1/openapi.json \
            | jq '.' > schemas/kanidm-openapi.json
          VERSION=$(jq -r '.info.version' schemas/kanidm-openapi.json)
          echo "SCHEMA_VERSION=${VERSION}" >> "$GITHUB_ENV"
          echo "Schema version: ${VERSION}"

      - name: Validate schema
        run: openapi-generator-cli validate -i schemas/kanidm-openapi.json

      - name: Generate client
        run: bash scripts/generate.sh

      - name: Update SchemaVersion constant
        run: |
          sed -i \
            "s/SchemaVersion = \".*\"/SchemaVersion = \"${SCHEMA_VERSION}\"/" \
            kanidm/version.go

      - name: Verify build
        run: go build ./...

      - name: Run unit tests
        run: go test -race ./kanidm/... ./internal/... ./tests/unit/...

      - name: Stop Kanidm container
        if: always()
        run: docker rm -f kanidm || true

      - name: Open PR if schema changed
        uses: peter-evans/create-pull-request@v8
        with:
          branch: chore/regenerate-client
          commit-message: "chore: regenerate client from Kanidm schema ${{ env.SCHEMA_VERSION }}"
          title: "chore: regenerate client from Kanidm schema ${{ env.SCHEMA_VERSION }}"
          body: |
            Auto-generated from OpenAPI schema version `${{ env.SCHEMA_VERSION }}`.

            **Review checklist:**
            - [ ] Breaking changes in generated models (`gen/kanidm/`)?
            - [ ] New endpoints need wrapper methods in `kanidm/`?
            - [ ] `SchemaVersion` constant updated correctly in `kanidm/version.go`?
            - [ ] Unit tests pass?
          labels: "auto-generated,dependencies"
          delete-branch: true
